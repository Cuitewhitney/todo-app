I used React + Vite, TypeScript and TailwindCSS to build a To-Do App.

A To-Do App that helps user to keep track of time 
takes in a user task for the month, week and daily; depending on the user input. 
a user can as well delete a task when they feel like and all
